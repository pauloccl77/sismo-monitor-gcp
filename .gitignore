# Credenciales y secrets — NUNCA commitear
*.json
!package.json
!package-lock.json
.env
.env.*
*-key.json
*-credentials.json
service-account*.json

# Terraform
terraform/.terraform/
terraform/terraform.tfstate
terraform/terraform.tfstate.backup
terraform/terraform.tfvars
*.tfvars
!*.tfvars.example

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
env/
*.egg-info/
dist/
build/

# IDEs
.vscode/
.idea/
*.swp
*.swo

# Artefactos del pipeline local
pipeline/output/
pipeline/test_events.json

# Estado local del poller
ingestion/state/seen_ids.json

# OS
.DS_Store
Thumbs.db

# Artefacto de build de Cloud Function
terraform/cf-notifier.zip

# Contexto interno Claude — no exponer en GitHub
.claude/
